<div id="auth">

    <div class="row h-100">
        <div class="col-lg-5 col-12">
            <div id="auth-left">
                <div class="auth-logo">
                    <a href="#">
                        <img src="~/images/logo/logo.png" alt="Logo">
                    </a>
                </div>
                <h1 class="auth-title">SPA with Rebels.</h1>
                <p class="auth-subtitle mb-5">
                    hot reload can apply everywhere but a slow a bit ya
                    We wait long time razor . This is not
                    blazor ya .. what you see
                    just xhr request :)
                </p>

                <form >
                    <div class="form-group position-relative has-icon-left mb-4">
                        <input type="text" class="form-control form-control-xl" placeholder="Username or email" id="beast">
                        <div class="form-control-icon">
                            <i class="bi bi-person"></i>
                        </div>
                    </div>
                    <div class="form-group position-relative has-icon-left mb-4">
                        <input type="password" class="form-control form-control-xl" placeholder="Password" id="beauty">
                        <div class="form-control-icon">
                            <i class="bi bi-shield-lock"></i>
                        </div>
                    </div>
                    <button type="button" class="btn btn-primary btn-block btn-lg shadow-lg mt-5" onclick="checkAccess()">Log in</button>
                </form>
            </div>
        </div>
        <div class="col-lg-7 d-none d-lg-block">
            <div id="auth-right">

            </div>
        </div>
    </div>

</div>
<script>
 function checkAccess() {
      $.ajax({
        type: 'POST',
        url: "42/web",
        data: {
            mode: 'web',
            beauty: $("#beast").val(),
            beast: $("#beauty").val(),
        },
        beforeSend: function () {
     
     
        }, success: function (data) {
            let status = data.status;
            let message = data.message;
            if (status) {
                window.location.href="Main";
            } else if (status === false) {
                Swal.fire("System", "Access Denied", "error");
            } else {
                Swal.fire("System", "access denied", "error");
            }
        }, error: function (xhr) {
            console.log(xhr.status);
            Swal.fire("System", "System Error", "error");
        }
    });
 }
</script>